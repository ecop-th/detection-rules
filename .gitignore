# ── ไฟล์ภายใน / สื่อการตลาด — ไม่ push ขึ้น public repo ──
_marketing/

# ── OS / editor junk ──
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# ── Python ──
__pycache__/
*.pyc
.venv/
venv/

# ── secret scan output ──
gitleaks-report.json
